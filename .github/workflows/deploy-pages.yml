name: Deploy to GitHub Pages

on:
  push:
    branches:
      - claude/clever-cori-8bobpv
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      # У GitHub Pages нет сервера, поэтому маршрут /api/contact,
      # требующий Node.js-рантайма, для статической сборки исключается.
      - name: Remove server-only API route for static export
        run: rm -rf src/app/api

      - name: Build static export
        run: npm run build
        env:
          GITHUB_PAGES_EXPORT: "true"
          NEXT_PUBLIC_STATIC_EXPORT: "true"

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
